server:
  port: 1025

spring:
  profiles:
    active: dubbo-standard

management:
  security:
    enabled: false
info:
  build:
    groupId: rui.framework
    artifact: spring-boot-dubbo-starter-parent
    name: dubbo 的 spring boot starter
    version: 0.0.1