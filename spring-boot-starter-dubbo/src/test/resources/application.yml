server:
  port: 1025

spring:
  profiles:
    active: dubbo-standard,dubbo-consumer,dubbo-provider

management:
  security:
    enabled: false
info:
  build:
    groupId: com.github.yinfujing
    artifact: spring-boot-dubbo-starter-parent
    name: dubbo 的 spring boot starter
    version: 0.0.1